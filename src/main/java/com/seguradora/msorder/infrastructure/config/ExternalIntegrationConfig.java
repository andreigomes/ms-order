package com.seguradora.msorder.infrastructure.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuração para beans de integração externa
 */
@Configuration
public class ExternalIntegrationConfig {
    // Removido o bean RestTemplate duplicado - já está definido em ApplicationConfig
}
